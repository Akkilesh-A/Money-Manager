const serverFailure = "Server error, Please try again later!"
const missingFields ="Required fields missing, check again!"
const missingUser="User not found!"
const verifyMail="Verify with OTP sent to mail!"

export const errorMessages={
    serverFailure,
    missingFields,
    missingUser,
    verifyMail
}